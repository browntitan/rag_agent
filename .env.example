# ==============================
# Backend selection
# ==============================
# Current supported backends:
# - DATABASE_BACKEND: postgres
# - VECTOR_STORE_BACKEND: pgvector
# Future-ready placeholders:
# - OBJECT_STORE_BACKEND: local | s3 | azure_blob
# - SKILLS_BACKEND: local | s3 | azure_blob
# - PROMPTS_BACKEND: local | s3 | azure_blob
DATABASE_BACKEND=postgres
VECTOR_STORE_BACKEND=pgvector
OBJECT_STORE_BACKEND=local
SKILLS_BACKEND=local
PROMPTS_BACKEND=local

# ==============================
# Provider selection
# ==============================
# LLM_PROVIDER: ollama | azure | nvidia
LLM_PROVIDER=azure
# EMBEDDINGS_PROVIDER: ollama | azure
EMBEDDINGS_PROVIDER=azure
# JUDGE_PROVIDER: ollama | azure | nvidia (defaults to LLM_PROVIDER if omitted)
JUDGE_PROVIDER=azure

# ==============================
# Ollama (default)
# ==============================
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=qwen3:8b
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_JUDGE_MODEL=qwen3:8b
OLLAMA_TEMPERATURE=0.2
OLLAMA_NUM_PREDICT=2048
# Demo-only generation budget override (used by `python run.py demo`)
DEMO_OLLAMA_NUM_PREDICT=2048

# Optional GGUF import helper (requires compose profiles: ollama + ollama-import)
# Place GGUF files + Modelfile in ./data/ollama/gguf
OLLAMA_GGUF_AUTO_IMPORT=false
OLLAMA_GGUF_MODEL_NAME=
OLLAMA_GGUF_MODELFILE=/gguf/Modelfile
JUDGE_TEMPERATURE=0.0

# ==============================
# Azure OpenAI (default demo path)
# ==============================
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE.openai.azure.us/
AZURE_OPENAI_API_VERSION=2024-05-01-preview
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o
AZURE_OPENAI_JUDGE_DEPLOYMENT=gpt-4o
AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT=text-embedding-ada-002
AZURE_TEMPERATURE=0.2

# Azure/vLLM HTTP + TLS behavior (corporate proxy/cert environments)
HTTP2_ENABLED=true
SSL_VERIFY=true
# Optional cert bundle path (PEM). Use quotes on Windows if path has spaces.
SSL_CERT_FILE=

# tiktoken behavior for embedding tokenization checks.
# If corporate SSL blocks tiktoken downloads, set TIKTOKEN_ENABLED=false.
TIKTOKEN_ENABLED=true
# Optional cache dir for pre-downloaded tiktoken files.
TIKTOKEN_CACHE_DIR=./data/cache/tiktoken

# ==============================
# NVIDIA OpenAI-compatible endpoint (chat/judge only)
# ==============================
NVIDIA_OPENAI_ENDPOINT=
NVIDIA_API_TOKEN=
# Backward-compatible alias accepted: Token
NVIDIA_CHAT_MODEL=openaigpt-oss-120b
NVIDIA_JUDGE_MODEL=openaigpt-oss-120b
NVIDIA_TEMPERATURE=0.0
# IMPORTANT: EMBEDDINGS_PROVIDER remains ollama|azure in v1 (nvidia embeddings unsupported)

# ==============================
# PostgreSQL / vector DB
# ==============================
# Host-run app (python run.py ...): use localhost DSN to reach postgres container.
# If you run the app via `docker compose`, this is auto-overridden to use
# `rag-postgres` service internally.
PG_DSN=postgresql://raguser:ragpass@localhost:5432/ragdb
EMBEDDING_DIM=1536

# Docker Compose DB variables (used by docker-compose.yml)
RAG_DB_NAME=ragdb
RAG_DB_USER=raguser
RAG_DB_PASSWORD=ragpass
RAG_DB_PORT=5432

# ==============================
# RAG & agent limits
# ==============================
MAX_AGENT_STEPS=10
MAX_TOOL_CALLS=12
MAX_RAG_AGENT_STEPS=8
RAG_MAX_RETRIES=2
RAG_MIN_EVIDENCE_CHUNKS=2
RAG_TOPK_VECTOR=12
RAG_TOPK_BM25=12

CHUNK_SIZE=900
CHUNK_OVERLAP=150

SUPERVISOR_MAX_LOOPS=5
MAX_PARALLEL_RAG_WORKERS=4
ENABLE_PARALLEL_RAG=true
CLEAR_SCRATCHPAD_PER_TURN=true

# ==============================
# Local data and source paths
# ==============================
# DATA_DIR controls default locations for KB/uploads/skills/prompts.
DATA_DIR=./data
KB_DIR=./data/kb
UPLOADS_DIR=./data/uploads

# Future-ready source URIs (currently local/file-based ingestion only)
KB_SOURCE_URI=file://./data/kb
UPLOADS_SOURCE_URI=file://./data/uploads

# ==============================
# Skills and prompt template paths
# ==============================
SKILLS_DIR=./data/skills
PROMPTS_DIR=./data/prompts

SHARED_SKILLS_PATH=./data/skills/skills.md
GENERAL_AGENT_SKILLS_PATH=./data/skills/general_agent.md
RAG_AGENT_SKILLS_PATH=./data/skills/rag_agent.md
SUPERVISOR_AGENT_SKILLS_PATH=./data/skills/supervisor_agent.md
UTILITY_AGENT_SKILLS_PATH=./data/skills/utility_agent.md
BASIC_CHAT_SKILLS_PATH=./data/skills/basic_chat.md

# Judge/synthesis prompt templates (tokenized text files)
JUDGE_GRADING_PROMPT_PATH=./data/prompts/judge_grading.txt
JUDGE_REWRITE_PROMPT_PATH=./data/prompts/judge_rewrite.txt
GROUNDED_ANSWER_PROMPT_PATH=./data/prompts/grounded_answer.txt
RAG_SYNTHESIS_PROMPT_PATH=./data/prompts/rag_synthesis.txt
PARALLEL_RAG_SYNTHESIS_PROMPT_PATH=./data/prompts/parallel_rag_synthesis.txt

# ==============================
# Docker app bootstrap behavior
# ==============================
WAIT_FOR_DB=true
DB_WAIT_TIMEOUT_SECONDS=90
APP_AUTO_MIGRATE=true
APP_AUTO_INIT_KB=false
OLLAMA_BASE_URL_DOCKER=http://ollama:11434
LANGFUSE_HOST_DOCKER=http://langfuse-web:3000
APP_API_PORT=8000

# ==============================
# OCR support (optional)
# ==============================
USE_PADDLE_OCR=true
OCR_LANGUAGE=en
OCR_USE_GPU=false
OCR_MIN_PAGE_CHARS=50

# ==============================
# Langfuse observability (optional)
# ==============================
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_DEBUG=false

# Langfuse self-host stack (docker compose --profile observability)
LANGFUSE_WEB_PORT=3000
LANGFUSE_POSTGRES_PASSWORD=postgres
LANGFUSE_REDIS_AUTH=myredissecret
LANGFUSE_CLICKHOUSE_PASSWORD=clickhouse
LANGFUSE_MINIO_ROOT_USER=minio
LANGFUSE_MINIO_ROOT_PASSWORD=miniosecret
LANGFUSE_MINIO_API_PORT=9090
LANGFUSE_MINIO_CONSOLE_PORT=9091
LANGFUSE_NEXTAUTH_SECRET=mysecret
LANGFUSE_SALT=mysalt
LANGFUSE_ENCRYPTION_KEY=00000000000000000000000000000000

# Optional automatic Langfuse bootstrap project/user
LANGFUSE_INIT_ORG_ID=
LANGFUSE_INIT_ORG_NAME=
LANGFUSE_INIT_PROJECT_ID=
LANGFUSE_INIT_PROJECT_NAME=
LANGFUSE_INIT_PROJECT_PUBLIC_KEY=
LANGFUSE_INIT_PROJECT_SECRET_KEY=
LANGFUSE_INIT_USER_EMAIL=
LANGFUSE_INIT_USER_NAME=
LANGFUSE_INIT_USER_PASSWORD=

# ==============================
# Request context defaults (CLI/demo compatibility)
# ==============================
DEFAULT_TENANT_ID=local-dev
DEFAULT_USER_ID=local-cli
DEFAULT_CONVERSATION_ID=local-session

# ==============================
# OpenAI-compatible gateway
# ==============================
GATEWAY_MODEL_ID=enterprise-agent
# No built-in API auth in simplified mode; secure at network/proxy layer if needed.
